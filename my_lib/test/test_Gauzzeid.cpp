#include "../my/CSR_matrix.h"

#include <gtest/gtest.h>
/*
#include <iostream>
TEST(CSR_matrix, gauzeid)
{
    vec<double> vals = {1, 2, 4, 2, 3, 5, 4, 5, 6};
    Matrix<double> M(vals , 3);
    vec<double> b = {1, 2, 3};
    vec<double> x0 = {1, 1, 1};
    vec<double> x = {17, 23, 32};
    CSR_matrix<double> A(M);
    double tol = 0.1;
    vec<double> res = Gauss_Zejdel_Method(A, b, x0, tol, 1000);
    EXPECT_EQ(true, max(x - res) < 1);
}*/
